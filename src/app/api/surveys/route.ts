export const dynamic = 'force-dynamic';
export const dynamic = 'force-dynamic'; import { NextResponse } from 'next/server'; import prisma from '@/lib/prisma';  export async function GET() {   try {     const surveys = await prisma.survey.findMany({       orderBy: { createdAt: 'desc' },       include: {         _count: {           select: { responses: true }         }       }     });      // Map to frontend expected format     const formattedSurveys = surveys.map(survey => ({       id: survey.id,       title: survey.title,       status: survey.status,       date: survey.createdAt.toLocaleDateString('pt-BR'),       responses: survey._count.responses     }));      return NextResponse.json(formattedSurveys);   } catch (error) {     return NextResponse.json({ error: 'Erro ao buscar pesquisas' }, { status: 500 });   } }  export async function POST(request: Request) {   try {     const body = await request.json();     const { id, title, status } = body;      const newSurvey = await prisma.survey.create({       data: {         id,         title,         status: status || 'Rascunho',         config: '[]' // empty sections initially       }     });      return NextResponse.json({       id: newSurvey.id,       title: newSurvey.title,       status: newSurvey.status,       date: newSurvey.createdAt.toLocaleDateString('pt-BR'),       responses: 0     });   } catch (error) {     return NextResponse.json({ error: 'Erro ao criar pesquisa' }, { status: 500 });   } }
